@page
@model YellowSalonWebsite.Pages.ContactUsModel
@{
}
<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            background-color: white;
            font-family: Arial, sans-serif;
        }

        h2 {
            color: #333;
        }

        form {
            max-width: 400px;
            margin: 0 auto;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        .rowElem {
            margin-bottom: 10px;
        }

        input[type="text"],
        input[type="email"],
        select,
        textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #333;
            border-radius: 5px;
            font-size: 16px;
        }

        select {
            height: 40px;
        }

        textarea {
            height: 60px;
        }

        select,
        input[type="text"],
        input[type="email"],
        textarea {
            color: #333;
        }

        input[type="submit"] {
            height: 40px;
            width: 100px;
            font-size: 22px;
            text-align: center;
            background: #333;
            color: #fff;
            border: none;
            cursor: pointer;
        }

            input[type="submit"]:hover {
                background: yellow;
                color: #333;
            }

        div {
            text-align: center;
            width: 100%;
        }

    </style>

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="~/css/Style.css" />

</head>
<body>


    <div>
        <iframe width="840" height="740" src="https://forms.office.com/r/08BdWiQK91?embed=true" frameborder="0" marginwidth="0"
                marginheight="0" style="border: none; max-width:100%; max-height:100vh" allowfullscreen webkitallowfullscreen mozallowfullscreen
                msallowfullscreen> </iframe>
    </div>
</body>
</html>

